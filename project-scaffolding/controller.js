export const userLogin = (request, response) => { //controller functions
    response.send("this is user login route")
 }
 
 export const userSignup = (request, response) => {
     response.send("this is user signup route")
 }